<h1>Cartitems#new</h1>
<p>Find me in app/views/cartitems/new.html.erb</p>

id:<%= @cartitem.product_id %>
<%= form_for @cartitem, url: {action: :create} do |f| %>

<p>
    
    商品名:<%= Product.find(@cartitem.product_id).name %><br>
    価格:<%= Product.find(@cartitem.product_id).price %>円<br>
    <%= f.label :qty, '数量' %>
    <%= f.text_field :qty %>
    
    <%= f.hidden_field :product_id, value: @product.id %>
    <%= f.hidden_field :cart_id, value: current_cart.id %>
    <%= f.submit '登録' %>
    数量:
</p>
<% end %>